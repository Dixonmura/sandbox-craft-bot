package vacancy_tracker.core;

public enum UserSettingState {
    NOT_INITIALIZED,
    SETTING_REGION,
    SETTING_MIN_EXPERIENCE,
    SETTING_MIN_SALARY,
    SETTING_KEYWORD,
    SETTING_NOTIFY_TIME,
    READY
}
