package vacancy_tracker.presentation.dto;

public enum CommandType {
    START,
    SET_UTC,
    SET_REGION,
    SET_MIN_EXPERIENCE,
    SET_MIN_SALARY,
    SET_KEYWORD,
    SET_NOTIFY_TIME,
    READY,
    STOP,
    UNKNOWN
}
